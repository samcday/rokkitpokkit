apiVersion: v1
kind: Service
metadata:
  name: rokkitpokkit
  namespace: rokkitpokkit
spec:
  selector:
    app: rokkitpokkit
  ports:
    - name: http
      port: 80
      protocol: TCP
      targetPort: http
